{
  "name": "studybug-server",
  "type": "module",
  "scripts": {
    "dev": "bun --watch run index.ts",
    "start": "bun run index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run db/migrate.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@clerk/backend": "^3.2.4",
    "@neondatabase/serverless": "^1.0.2",
    "drizzle-orm": "^0.45.2",
    "resend": "^6.10.0",
    "svix": "^1.90.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.4.3",
    "bun-types": "^1.3.11",
    "drizzle-kit": "^0.31.10"
  }
}
